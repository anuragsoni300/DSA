#include <bits/stdc++.h>
#include "utils/common.h"
using namespace std;
using ll = long long;

class Solution
{
public:
    // vector<vector<int>> threeSum(vector<int> &nums)
    // {
    // }
};

int main()
{
    vector<int> v = {-1, 0, 1, 2, -1, -4};
    vector<vector<int>> cc = {{-1, -1, 2}, {-1, 0, 1}};
    Solution s = Solution();
    DisplayMap(cc);
    return 0;
}